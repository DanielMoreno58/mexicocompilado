<div class="row">
  <%= render 'layouts/menu' %>
  <div class="offset-3 col-7">
    <div class="row">
      <h2><%= @question.question_name %></h2>
    </div>
    <div class="row">
      <%= @question.content %>
    </div>
    <hr class="row">
    <div class="row mt-5">
      <h3>Respuestas</h3>
      <!-- Aqui van las respuestas -->
    </div>
    <hr class="row">
    <div class="row mt-5">
      <h3>¿Sabes como solucionarlo?</h3>
    </div>
    <div class="row">
      <%= form_for [@question, @question.answers.build], url: foro_answers_path(@question.id) do |f| %>
        <%= f.text_area :answer_description, rows: '5', cols: '100', class: 'form-control', placeholder: 'Agrega una respuesta' %>
        <div class="row mt-2 mr-0">
          <%= f.submit "Responder", class: 'offset-10 col-2 btn btn-primary' %>
        </div>
      <% end %>
    </div>
  </div>
</div>
