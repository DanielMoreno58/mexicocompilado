<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Mexicocompilado Rails Website" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Mexicocompilado Rails Website" %>">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => 'reload' %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <header>
      <%= render 'layouts/header' %>
    </header>
    <main role="main">
       <%= render 'layouts/messages' %>

       <% if current_page?(controller: 'foro', action: 'index') %>
        <div>
          <%= image_tag("foro.png", :alt => "banner foro") %>
        </div>
       <% end %>

       <div class="container py-5">
          <%= yield %>
       </div>   
    </main>
    <%= render 'layouts/footer' %>
  </body>
</html>
